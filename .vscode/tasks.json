{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deploy ARM template",
            "type": "shell",
            "command": "cmd < deploy.azcli",
            "options": {
                "cwd": "${workspaceFolder}/scripts"
            },
            "problemMatcher": []

        }
    ]
}